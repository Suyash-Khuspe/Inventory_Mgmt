@model Inventory_Mgmt.Models.sale

@{
    ViewBag.Title = "Delete Sale";
}

<div class="container mt-5">
    <div class="card border-danger shadow-sm">
        <div class="card-header bg-danger text-white text-center">
            <h3>Are you sure you want to delete this sale?</h3>
        </div>

        <div class="card-body">
            <h4 class="card-title text-center mb-4">Sale Details</h4>
            <dl class="row">
                <dt class="col-sm-4 text-right">
                    @Html.DisplayName("Sale Product")
                </dt>
                <dd class="col-sm-8">
                    <p class="form-control-plaintext">@Html.DisplayFor(model => model.sale_prod)</p>
                </dd>

                <dt class="col-sm-4 text-right">
                    @Html.DisplayName("Sale Quantity")
                </dt>
                <dd class="col-sm-8">
                    <p class="form-control-plaintext">@Html.DisplayFor(model => model.sale_qnty)</p>
                </dd>

                <dt class="col-sm-4 text-right">
                    @Html.DisplayName("Sale Date")
                </dt>
                <dd class="col-sm-8">
                    <p class="form-control-plaintext">@Html.DisplayFor(model => model.sale_date)</p>
                </dd>
            </dl>
        </div>

        <div class="card-footer">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="row">
                    <div class="col-md-1">
                        <input type="submit" value="Delete" class="btn btn-outline-danger btn-block" />
                    </div>
                    <div class="col-md-1">
                        @Html.ActionLink("Cancel", "DisplaySale", "Sale", null, new { @class = "btn btn-outline-secondary btn-block" })
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
